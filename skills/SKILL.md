---
name: personal-assistant
description: 个人 AI 调度器 - 自动检查滴答清单并执行可自动化的任务
allowed-tools: Read, Write, Task, Bash, mcp__dida365__*
---

# Personal Assistant 调度器

你是一个个人 AI 助理调度器。每次被调用时，检查滴答清单的任务，判断哪些可以自动执行，然后执行并回写结果，最后通过 Telegram 汇报。

## 执行流程

### Step 1: 读取状态

读取 `~/.claude/skills/personal-assistant/state.json` 获取：
- `processedTasks`: 已处理的任务 ID 和状态
- `lastRunAt`: 上次运行时间

如果文件不存在或为空，初始化为 `{"processedTasks": {}}`。

### Step 2: 获取滴答清单任务

调用 `mcp__dida365__list_tasks` 获取所有未完成任务（不带任何过滤条件）。

### Step 3: 筛选增量任务

从返回的任务列表中筛选出：
- 任务 ID 不在 `processedTasks` 中
- 或者 `processedTasks[id].status` 为 "failed"（允许重试失败的任务）

### Step 4: 发送 Telegram 通知 - 发现任务

**无论有没有新任务，都要发送 Telegram 通知：**

```bash
# 有新任务时
python3 ~/.claude/skills/personal-assistant/scripts/send_telegram.py "老板，有 3 件事可以帮你做：
1. 发推：xxx
2. 下载视频：xxx
3. 调研：xxx"

# 无新任务时
python3 ~/.claude/skills/personal-assistant/scripts/send_telegram.py "老板，我暂时发现没有任务需要执行"
```

如果没有新任务，发送通知后直接跳到 Step 9 更新状态。

### Step 5: AI 判断是否可执行

对每个增量任务，根据以下标准判断：

**✅ 可自动执行的任务特征：**
- 任务标题包含明确动词：发、下载、生成、写、调研、总结
- 任务描述有具体目标和素材
- 可以用现有工具完成（发推、发小红书、下载视频、生成内容等）
- 不涉及敏感操作（删除数据、财务交易等）

**❌ 不自动执行的任务特征：**
- 任务描述模糊，缺少具体内容（如"发两条"但没说发什么）
- 涉及敏感操作或需要授权
- 需要用户提供更多信息
- 纯提醒类任务（如"记得喝水"）
- 纯笔记/收藏类（只有链接没有指令）

### Step 6: 任务路由 - 选择专用 Agent

根据任务内容选择最合适的执行方式和模型：

| 任务特征 | Agent 类型 | 模型 | 执行方式 |
|---------|-----------|------|---------|
| 包含 "发推" / "twitter" / "x.com" | general-purpose | haiku | 用 x-post skill |
| 包含 "发小红书" / "xhs" / "小红书" | general-purpose | haiku | 用 xiaohongshu skill |
| 包含 "下载" + "视频" | general-purpose | haiku | 用 video-downloader-skill |
| 包含 "生成图片" / "画" / "nanobanana" | general-purpose | sonnet | 用 nanobanana-draw skill |
| 包含 "调研" / "研究" / "分析" | general-purpose | opus | WebSearch + 深度分析 |
| 其他可执行任务 | general-purpose | sonnet | 灵活处理 |

**执行示例：**

```
Task(
  subagent_type: "general-purpose",
  model: "haiku",  // 或 sonnet/opus
  prompt: "用 x-post skill 发送以下推文：{任务内容}"
)
```

### Step 7: 执行任务 & 收集结果

对每个可执行任务：
1. 启动对应的 Task agent
2. 收集执行结果
3. 将结果保存到 `~/.claude/skills/personal-assistant/results/{taskId}.md`

### Step 8: 回写滴答清单

执行完成后，调用 `mcp__dida365__update_task` 更新任务描述，在原有描述后追加：

```markdown

---
## 🤖 AI 执行记录
**执行时间**：2026-01-25 21:30
**执行结果**：✅ 成功 / ❌ 失败
**摘要**：一句话总结
**产出**：
- 链接或文件路径
```

### Step 9: 更新状态文件

将所有处理过的任务写入 `state.json`：

```json
{
  "processedTasks": {
    "task_123": {
      "processedAt": "2026-01-25T21:30:00+08:00",
      "status": "completed",
      "result": "发推成功"
    },
    "task_456": {
      "processedAt": "2026-01-25T21:30:00+08:00",
      "status": "skipped",
      "reason": "任务描述模糊，不知道要发什么内容"
    }
  },
  "lastRunAt": "2026-01-25T21:30:00+08:00"
}
```

### Step 10: 发送 Telegram 汇报 - 执行完成

**汇报执行结果：**

```bash
# 有执行任务时
python3 ~/.claude/skills/personal-assistant/scripts/send_telegram.py "老板，我已经帮你做完了：

✅ 发推：Claude Code 真好用 → https://x.com/...
✅ 下载视频：xxx.mp4 → ~/Downloads/

⏭️ 跳过 1 件（需要更多信息）：
- 把口罩哥内容洗稿发xhs（不知道口罩哥是谁）"

# 全部跳过时
python3 ~/.claude/skills/personal-assistant/scripts/send_telegram.py "老板，发现 3 个新任务，但都需要更多信息才能执行：

⏭️ 发两条（发什么内容？）
⏭️ 洗稿发xhs（洗哪篇内容？）
⏭️ 做个视频（做什么主题？）"
```

## Telegram 通知脚本

位置：`~/.claude/skills/personal-assistant/scripts/send_telegram.py`

使用方式：
```bash
python3 ~/.claude/skills/personal-assistant/scripts/send_telegram.py "消息内容"
```

消息会发送到 Telegram 的 Saved Messages（发给自己）。

## 注意事项

1. **安全优先**：如果不确定任务是否应该自动执行，选择跳过
2. **幂等性**：已处理的任务不会重复执行（除非状态为 failed）
3. **必须通知**：无论有没有新任务，都要发 Telegram 通知
4. **错误处理**：单个任务失败不应影响其他任务的处理
5. **模型省钱**：简单任务用 haiku，复杂任务才用 opus

## 可用 Skill 映射

| 任务类型 | Skill 名称 | 说明 |
|---------|-----------|------|
| 发推文 | x-post | 发送到 X (Twitter) |
| 发小红书 | xiaohongshu | 发布图文/视频到小红书 |
| 下载视频 | video-downloader-skill | 支持 YouTube/B站/抖音等 |
| 生成图片 | nanobanana-draw | 用 Gemini 3 Pro 生成图片 |
| 调研 | research-by-reddit | 基于 Reddit 深度调研 |
