[project]
name = "supabase-upload"
version = "0.1.0"
description = "Supabase Storage upload tool"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
